FROM node:20-bookworm-slim

RUN apt-get update \
	&& apt-get install -y --no-install-recommends bash ca-certificates curl git \
	&& npm install -g @openai/codex \
	&& rm -rf /var/lib/apt/lists/*

WORKDIR /agent

ENTRYPOINT ["/bin/bash", "/agent/run-agent.sh"]
