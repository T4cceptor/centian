prompt: |
  Connect to the MCP server named `centian` and solve this Go benchmark case through the Centian workflow.
  Register the `simple_tdd` template and use Centian lifecycle tools as the source of truth.

  This benchmark already has an authored red baseline.

  Requirements:
  - Verify the authored red baseline before implementing code.
  - Do not create, rewrite, or edit the locked baseline test file `internal/score/score_test.go`.
  - Make the selected command pass by changing production code and any supporting non-locked files.
  - After the selected command is green, perform one small cleanup or refactor if appropriate, while keeping the selected command green.
  - Stop after the task is completed.
